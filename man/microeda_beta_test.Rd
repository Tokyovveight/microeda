% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/beta-test.R
\name{microeda_beta_test}
\alias{microeda_beta_test}
\title{Test beta diversity groups with paired dispersion diagnostics}
\usage{
microeda_beta_test(x, permutations = 999, seed = NULL)
}
\arguments{
\item{x}{A grouped \code{microeda_beta} object.}

\item{permutations}{Number of permutations passed to \code{vegan}.}

\item{seed}{Optional random seed used while running the permutation tests.}
}
\value{
A \code{microeda_beta_test} object with PERMANOVA results, dispersion
diagnostics, caveats, parameters, and matched call.
}
\description{
\code{microeda_beta_test()} runs a PERMANOVA-style grouped distance test together
with betadisper-style dispersion diagnostics using the stored distance object
from a \code{microeda_beta} result. The test is exploratory: PERMANOVA can be
confounded by group dispersion differences and should not be interpreted
without the dispersion diagnostics.
}
\examples{
counts <- matrix(
  c(
    10, 0, 0,
    8, 1, 0,
    0, 9, 1,
    0, 7, 2
  ),
  nrow = 4,
  byrow = TRUE
)
rownames(counts) <- paste0("S", 1:4)
colnames(counts) <- paste0("ASV", 1:3)
metadata <- data.frame(group = c("A", "A", "B", "B"), row.names = rownames(counts))

beta <- microeda_beta(
  counts,
  metadata = metadata,
  group = "group",
  taxa_are_rows = FALSE
)
if (requireNamespace("vegan", quietly = TRUE)) {
  beta_test <- microeda_beta_test(beta, permutations = 99, seed = 1)
  as_beta_test_summary(beta_test)
  cat(microeda_beta_test_report(beta_test))
}
}
