% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/da-comparison.R
\name{as_da_comparison}
\alias{as_da_comparison}
\title{Align standardized DA results side by side}
\usage{
as_da_comparison(
  x,
  contrast = NULL,
  methods = NULL,
  alpha = 0.05,
  features = NULL
)
}
\arguments{
\item{x}{A \code{microeda_da} object.}

\item{contrast}{Optional contrast labels. \code{NULL} selects all contrasts.}

\item{methods}{Optional method IDs. \code{NULL} selects all methods.}

\item{alpha}{Strictly positive adjusted p-value threshold below one. It is
used only for each method's descriptive \code{p_adjusted_le_alpha} field.}

\item{features}{Optional feature IDs to retain.}
}
\value{
A base data frame with identifier columns followed by one prefixed
column block per selected method. Native p-values, adjusted p-values,
significance labels, and effects are not recalculated.
}
\description{
\code{as_da_comparison()} creates one row per feature and contrast and aligns
selected method-specific standardized outputs in prefixed column blocks.
It is a descriptive view, not a consensus analysis or method ranking.
}
\details{
ALDEx2 effects retain the native ALDEx2 scale. ANCOM-BC2 effects are native
natural-log estimates for \code{group2 - group1}. DESeq2 effects are native
unshrunk log2 fold changes for \code{group2 / group1}. Signs share the
group2-relative orientation, but effect magnitudes are not directly
comparable across methods.

Identifier columns are \code{feature_id}, \code{taxon_label}, \code{rank}, \code{contrast},
\code{group1}, and \code{group2}. Each selected method adds prefixed \code{effect},
\code{effect_type}, \code{p_value}, \code{p_adjusted}, \code{significance}, \code{effect_sign},
\code{tested}, and \code{p_adjusted_le_alpha} columns.

\code{tested} is \code{TRUE} when the method returned the feature with a non-missing
native p-value, \code{FALSE} when the row exists but its native p-value is
missing, and \code{NA} when the backend did not return that feature for the
contrast. Missing values are not converted to non-significant results.
}
\examples{
\dontrun{
comparison <- as_da_comparison(da, contrast = "A_vs_B")
}

}
\seealso{
\code{\link[=as_da_results]{as_da_results()}}, \code{\link[=as_da_summary]{as_da_summary()}}, \code{\link[=as_da_raw_output]{as_da_raw_output()}},
\code{\link[=microeda_da_comparison_report]{microeda_da_comparison_report()}}, \code{\link[=write_da_comparison]{write_da_comparison()}}
}
