% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc-target-composition.R
\name{as_qc_target_composition}
\alias{as_qc_target_composition}
\title{Extract target-versus-non-target QC composition tables}
\usage{
as_qc_target_composition(
  x,
  level = c("overall", "kingdom", "sample", "feature")
)
}
\arguments{
\item{x}{A \code{microeda_qc} object created with a non-\code{NULL}
\code{target_kingdom}.}

\item{level}{Table to return: \code{"overall"}, \code{"kingdom"}, \code{"sample"}, or
\code{"feature"}.}
}
\value{
A base data frame. \code{"overall"} has one row; the other levels have
one row per kingdom label, sample, or feature.
}
\description{
\code{as_qc_target_composition()} returns one target-composition table created
by \code{\link[=microeda_qc]{microeda_qc()}}. The diagnostic uses raw counts and separates target,
known non-target, and unclassified kingdom assignments. It does not filter
features or establish contamination. In normalized matching mode, only
leading \code{d__}, \code{k__}, \code{domain__}, \code{kingdom__}, and \code{D_0__} prefixes are
removed before case-insensitive comparison; fuzzy and substring matching
are not used.
}
\details{
The sample-level \code{dominant_non_target_read_proportion} is the dominant known
non-target kingdom's reads divided by all reads in that sample. A
zero-library sample therefore has \code{NA_real_} proportions. The kingdom-level
\code{proportion_within_non_target_reads} is defined only for known non-target
labels and is \code{NA_real_} when no non-target reads are present.
}
\seealso{
\code{\link[=microeda_qc]{microeda_qc()}}, \code{\link[=microeda_qc_report]{microeda_qc_report()}}, \code{\link[=microeda_qc_plot]{microeda_qc_plot()}}
}
