% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/beta.R
\name{microeda_beta_compare}
\alias{microeda_beta_compare}
\title{Compare beta diversity distance methods}
\usage{
microeda_beta_compare(
  x,
  metadata = NULL,
  group = NULL,
  taxa_are_rows = TRUE,
  methods = c("bray", "jaccard", "hellinger")
)
}
\arguments{
\item{x}{A \code{phyloseq} object, matrix, or data frame containing counts.}

\item{metadata}{Optional sample metadata. Required for non-phyloseq inputs
when group-level diagnostics are desired.}

\item{group}{Optional metadata column used for group-size diagnostics and
group-aware recommendations.}

\item{taxa_are_rows}{For matrix/data frame inputs, whether rows are taxa.
Ignored for \code{phyloseq} input because the orientation is read from the
object.}

\item{methods}{Distance methods to compute. Supported values are \code{"bray"},
\code{"jaccard"}, and \code{"hellinger"}.}
}
\value{
A \code{microeda_beta_compare} object containing named \code{microeda_beta}
results, method names, sample IDs, optional group information, count-type
diagnostics, and matched call.
}
\description{
\code{microeda_beta_compare()} computes multiple \code{microeda_beta} results from the
same input so distance summaries can be compared consistently.
}
\examples{
counts <- matrix(
  c(
    1, 2, 0,
    2, 1, 0,
    0, 0, 3
  ),
  nrow = 3,
  byrow = TRUE
)
rownames(counts) <- c("S1", "S2", "S3")
colnames(counts) <- paste0("ASV", 1:3)

beta_cmp <- microeda_beta_compare(counts, taxa_are_rows = FALSE)
as_beta_compare_summary(beta_cmp)
}
