% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/da.R
\name{write_da_results}
\alias{write_da_results}
\title{Write standardized differential representation results to CSV}
\usage{
write_da_results(x, file, na = "", quote = TRUE)
}
\arguments{
\item{x}{A \code{microeda_da} object.}

\item{file}{Single non-empty character path for the output CSV file.}

\item{na}{String used for missing values in the CSV.}

\item{quote}{Logical; passed to \code{utils::write.csv()}.}
}
\value{
The output file path, invisibly.
}
\description{
\code{write_da_results()} writes only the standardized result table returned by
\code{as_da_results(x)}. Raw backend outputs are not exported or serialized by
this helper; they remain available in \code{x$raw_outputs}.
}
\details{
Exported rows are exploratory method outputs, not confirmed biological
discoveries.
}
\examples{
counts <- matrix(
  c(40, 8, 20, 2,
    38, 9, 22, 3,
    42, 7, 19, 2,
    12, 35, 18, 4,
    10, 37, 17, 5,
    11, 33, 20, 4),
  nrow = 6,
  byrow = TRUE
)
rownames(counts) <- paste0("S", seq_len(6))
colnames(counts) <- paste0("ASV", seq_len(4))
metadata <- data.frame(
  group = c("A", "A", "A", "B", "B", "B"),
  row.names = rownames(counts)
)

if (requireNamespace("ALDEx2", quietly = TRUE)) {
  da <- microeda_da(
    counts,
    metadata = metadata,
    group = "group",
    contrast = c("A", "B"),
    taxa_are_rows = FALSE,
    mc.samples = 16
  )
  write_da_results(da, tempfile(fileext = ".csv"))
}

}
\seealso{
\code{\link[=as_da_results]{as_da_results()}}, \code{\link[=write_da_comparison]{write_da_comparison()}}
}
