% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alpha.R
\name{microeda_alpha_report}
\alias{microeda_alpha_report}
\title{Build a compact alpha diversity report}
\usage{
microeda_alpha_report(x, alpha_compare = NULL, digits = 3)
}
\arguments{
\item{x}{A \code{microeda_alpha} object.}

\item{alpha_compare}{Optional \code{microeda_alpha_compare} object from
\code{\link[=microeda_alpha_compare]{microeda_alpha_compare()}}.}

\item{digits}{Number of digits used when formatting numeric values.}
}
\value{
A single character string suitable for \code{\link[=cat]{cat()}}.
}
\description{
\code{microeda_alpha_report()} formats alpha diversity group summaries and,
optionally, alpha group tests into a compact plain-text report. It does not
change the machine-readable extractor tables returned by
\code{\link[=as_alpha_summary]{as_alpha_summary()}} or \code{\link[=as_alpha_tests]{as_alpha_tests()}}.
}
\examples{
counts <- matrix(
  c(
    10, 0, 0, 5,
    20, 0, 1, 0,
    0, 4, 0, 0,
    2, 3, 0, 1
  ),
  nrow = 4,
  byrow = TRUE
)
rownames(counts) <- paste0("S", 1:4)
colnames(counts) <- paste0("ASV", 1:4)
metadata <- data.frame(group = c("A", "A", "B", "B"), row.names = rownames(counts))

alpha <- microeda_alpha(
  counts,
  metadata = metadata,
  group = "group",
  taxa_are_rows = FALSE
)
alpha_cmp <- microeda_alpha_compare(alpha, indices = c("observed", "shannon"))
cat(microeda_alpha_report(alpha, alpha_compare = alpha_cmp))
}
