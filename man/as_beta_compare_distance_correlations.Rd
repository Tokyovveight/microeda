% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/beta-compare.R
\name{as_beta_compare_distance_correlations}
\alias{as_beta_compare_distance_correlations}
\title{Correlate beta diversity distance methods}
\usage{
as_beta_compare_distance_correlations(x, correlation_method = "spearman")
}
\arguments{
\item{x}{A \code{microeda_beta_compare} object.}

\item{correlation_method}{Correlation method. Supported values are
\code{"pearson"}, \code{"spearman"}, and \code{"kendall"}.}
}
\value{
A data frame with one row per method pair.
}
\description{
\code{as_beta_compare_distance_correlations()} computes descriptive correlations
between pairwise distance vectors from a \code{microeda_beta_compare} object.
}
\details{
Distance-vector correlations are descriptive only. They do not identify the
correct method and do not replace PERMANOVA, dispersion checks, or
compositional diagnostics.
}
\examples{
counts <- matrix(
  c(
    1, 2, 0,
    2, 1, 0,
    0, 0, 3
  ),
  nrow = 3,
  byrow = TRUE
)
rownames(counts) <- c("S1", "S2", "S3")
colnames(counts) <- paste0("ASV", 1:3)

beta_cmp <- microeda_beta_compare(counts, taxa_are_rows = FALSE)
as_beta_compare_distance_correlations(beta_cmp)
}
