% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/da.R
\name{microeda_da}
\alias{microeda_da}
\title{Run exploratory differential representation with ALDEx2}
\usage{
microeda_da(
  x,
  metadata = NULL,
  taxonomy = NULL,
  group,
  contrast = "pairwise",
  methods = "aldex2",
  tax_rank = NULL,
  prevalence_filter = NULL,
  min_count = NULL,
  p_adjust_method = NULL,
  taxa_are_rows = TRUE,
  mc.samples = 128,
  denom = "all",
  paired.test = FALSE
)
}
\arguments{
\item{x}{A \code{phyloseq} object, count matrix, or count data frame.}

\item{metadata}{Optional sample metadata when \code{x} is a count table.}

\item{taxonomy}{Optional taxonomy table.}

\item{group}{Metadata column containing group labels.}

\item{contrast}{Either a length-2 character vector of group levels or
\code{"pairwise"} to run all pairwise group comparisons.}

\item{methods}{Differential representation methods to run. Only
\code{"aldex2"} is implemented in this version.}

\item{tax_rank}{Optional taxonomy rank used for \code{taxon_label}.}

\item{prevalence_filter}{Optional filter value recorded for future use.
This wrapper does not apply filtering.}

\item{min_count}{Optional count filter recorded for future use. This wrapper
does not apply filtering.}

\item{p_adjust_method}{Optional future backend override request. \code{NULL}
keeps backend-native/default p-value adjustment.}

\item{taxa_are_rows}{Logical; for count tables, whether taxa/features are
rows.}

\item{mc.samples}{Number of Monte Carlo samples passed to ALDEx2.}

\item{denom}{Denominator strategy passed to \code{ALDEx2::aldex.clr()}.}

\item{paired.test}{Logical passed to ALDEx2 test/effect helpers.}
}
\value{
A \code{microeda_da} object containing standardized results, method
results, preserved raw backend outputs, caveats, and parameters.
}
\description{
\code{microeda_da()} runs a cautious differential representation workflow for
microbiome count data. In this version, the only implemented backend is
ALDEx2. Results are standardized for downstream use while preserving the raw
backend output in the returned object.
}
\details{
This helper is exploratory: it uses method-native p-value adjustment, does
not globally re-adjust backend outputs, does not rank methods, and does not
claim ground-truth differentially abundant taxa.
}
\examples{
counts <- matrix(
  c(40, 8, 20, 2,
    38, 9, 22, 3,
    42, 7, 19, 2,
    12, 35, 18, 4,
    10, 37, 17, 5,
    11, 33, 20, 4),
  nrow = 6,
  byrow = TRUE
)
rownames(counts) <- paste0("S", seq_len(6))
colnames(counts) <- paste0("ASV", seq_len(4))
metadata <- data.frame(
  group = c("A", "A", "A", "B", "B", "B"),
  row.names = rownames(counts)
)

if (requireNamespace("ALDEx2", quietly = TRUE)) {
  da <- microeda_da(
    counts,
    metadata = metadata,
    group = "group",
    contrast = c("A", "B"),
    taxa_are_rows = FALSE,
    mc.samples = 16
  )
  as_da_results(da)
}

}
