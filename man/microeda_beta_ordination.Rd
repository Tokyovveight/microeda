% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/beta-ordination.R
\name{microeda_beta_ordination}
\alias{microeda_beta_ordination}
\title{Ordinate beta diversity distances}
\usage{
microeda_beta_ordination(x, method = "pcoa", dimensions = 2)
}
\arguments{
\item{x}{A \code{microeda_beta} object.}

\item{method}{Ordination method. Only \code{"pcoa"} is currently supported.}

\item{dimensions}{Number of ordination axes to return.}
}
\value{
A \code{microeda_beta_ordination} object with coordinates, eigenvalues,
variance explained, method metadata, optional group information, and matched
call.
}
\description{
\code{microeda_beta_ordination()} computes a minimal principal coordinates
analysis (PCoA) ordination from a \code{microeda_beta} distance object using
\code{\link[stats:cmdscale]{stats::cmdscale()}}.
}
\examples{
counts <- matrix(
  c(
    1, 2, 0,
    2, 1, 0,
    0, 0, 3
  ),
  nrow = 3,
  byrow = TRUE
)
rownames(counts) <- c("S1", "S2", "S3")
colnames(counts) <- paste0("ASV", 1:3)

beta <- microeda_beta(counts, taxa_are_rows = FALSE)
ord <- microeda_beta_ordination(beta)
as_beta_coordinates(ord)
}
