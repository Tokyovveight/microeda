% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/beta-test.R
\name{microeda_beta_compare_test}
\alias{microeda_beta_compare_test}
\title{Test beta diversity groups across compared distance methods}
\usage{
microeda_beta_compare_test(x, permutations = 999, seed = NULL)
}
\arguments{
\item{x}{A grouped \code{microeda_beta_compare} object.}

\item{permutations}{Number of permutations passed to \code{vegan} for each
method.}

\item{seed}{Optional random seed used while running each method's
permutation tests.}
}
\value{
A \code{microeda_beta_compare_test} object containing one
\code{microeda_beta_test} result per method, aggregated caveats, parameters,
and matched call.
}
\description{
\code{microeda_beta_compare_test()} runs \code{\link[=microeda_beta_test]{microeda_beta_test()}} for each
\code{microeda_beta} object stored in a grouped \code{microeda_beta_compare} object.
The results are side-by-side exploratory diagnostics, not a formal ranking
of distance methods.
}
\examples{
counts <- matrix(
  c(
    10, 0, 0,
    8, 1, 0,
    0, 9, 1,
    0, 7, 2
  ),
  nrow = 4,
  byrow = TRUE
)
rownames(counts) <- paste0("S", 1:4)
colnames(counts) <- paste0("ASV", 1:3)
metadata <- data.frame(group = c("A", "A", "B", "B"), row.names = rownames(counts))

beta_cmp <- microeda_beta_compare(
  counts,
  metadata = metadata,
  group = "group",
  taxa_are_rows = FALSE,
  methods = c("bray", "jaccard")
)
if (requireNamespace("vegan", quietly = TRUE)) {
  beta_cmp_test <- microeda_beta_compare_test(beta_cmp, permutations = 99, seed = 1)
  as_beta_compare_test_summary(beta_cmp_test)
  cat(microeda_beta_compare_test_report(beta_cmp_test))
}
}
