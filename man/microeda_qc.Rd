% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{microeda_qc}
\alias{microeda_qc}
\title{Build a tidy QC summary from a phyloseq object or count matrix}
\usage{
microeda_qc(
  x,
  metadata = NULL,
  taxonomy = NULL,
  group = NULL,
  taxa_are_rows = TRUE,
  min_prevalence = 0.05,
  target_kingdom = NULL,
  kingdom_rank = NULL,
  target_match = c("normalized", "exact")
)
}
\arguments{
\item{x}{A \code{phyloseq} object, matrix, or data frame containing counts.}

\item{metadata}{Optional sample metadata. Required for non-phyloseq inputs
when group-level diagnostics are desired.}

\item{taxonomy}{Optional taxonomy table with features as rows.}

\item{group}{Optional metadata column used for group-size diagnostics and
broad group-aware screening notes.}

\item{taxa_are_rows}{For matrix/data frame inputs, whether rows are taxa.
Ignored for \code{phyloseq} input because the orientation is read from the
object.}

\item{min_prevalence}{Features with sample prevalence strictly below this
threshold are reported but flagged as \code{above_prevalence_threshold = FALSE}
in the per-feature table. The per-sample \code{n_features_above_prevalence}
count is restricted to features at or above the threshold.}

\item{target_kingdom}{Optional non-empty character vector of target kingdom
labels. When supplied, target, known non-target, and unclassified
composition is summarized from raw counts without filtering features or
samples.}

\item{kingdom_rank}{Optional taxonomy column used for target matching.
Matching is case-insensitive. When \code{NULL}, \code{Kingdom}, \code{Domain}, then
\code{Superkingdom} are tried in that order.}

\item{target_match}{Kingdom-label matching mode. \code{"normalized"} trims
whitespace, compares case-insensitively, and removes only recognized
leading rank prefixes. \code{"exact"} trims whitespace but otherwise preserves
labels for matching.}
}
\value{
A \code{microeda_qc} object with the following tidy data frames and
compact summaries:
\describe{
\item{\code{per_sample}}{One row per sample with \code{sample_id}, \code{library_size},
\code{zero_fraction}, \code{n_features_detected}, \code{n_features_above_prevalence}.}
\item{\code{per_feature}}{One row per feature with \code{feature_id}, \code{total_reads},
\code{prevalence}, \code{n_samples_detected}, \code{above_prevalence_threshold}.}
\item{\code{library_size_summary}}{A named list with compact library-size
diagnostics, including total reads, quartiles, zero-library samples,
and imbalance ratios.}
\item{\code{sparsity_summary}}{A named list with matrix-wide zero fraction,
zero-library samples, zero-abundance features, and median sample/feature
zero fractions.}
\item{\code{prevalence_summary}}{A named list with compact prevalence and
filtering diagnostics, including features above and below the
\code{min_prevalence} threshold.}
\item{\code{feature_dominance}}{A named list with read fractions explained by
the top 10, 50, and 100 features, plus feature counts needed to explain
at least 50\% and 90\% of reads.}
\item{\code{qc_flags}}{A data frame of conservative QC flags. Empty when no
simple library-size, sparsity, or prevalence flags are triggered.}
\item{\code{qc_observations}}{A data frame of concise human-readable QC
observations derived from the compact summaries and QC flags.}
\item{\code{per_rank}}{One row per taxonomy rank with \code{rank}, \code{n_assigned},
\code{n_unique}, \code{missing_fraction}. \code{NULL} when no taxonomy is supplied.}
\item{\code{metadata_completeness}}{One row per metadata column with
\code{column}, \code{missing_fraction}, \code{n_unique}, \code{is_constant}, \code{is_group}.
\code{NULL} when no metadata is supplied.}
\item{\code{target_composition}}{Target-versus-non-target composition tables,
observations, and caveats. \code{NULL} when \code{target_kingdom = NULL}.}
\item{\code{params}}{A named list of analysis parameters used to produce the
QC object, including prevalence and target-composition settings.}
\item{\code{call}}{The matched function call.}
}
}
\description{
\code{microeda_qc()} returns tidy per-sample, per-feature, per-rank, and
metadata-completeness tables derived directly from the extracted count,
metadata, and taxonomy data. It does not run the rule engine, so for
diagnostics and recommendations use \code{\link[=microeda_check]{microeda_check()}}. When
\code{target_kingdom} is supplied, it also describes target, known non-target,
and unclassified taxonomy signal without removing any data. Known
non-target signal is not automatically evidence of contamination.
}
\seealso{
\code{\link[=as_qc_target_composition]{as_qc_target_composition()}}, \code{\link[=microeda_qc_report]{microeda_qc_report()}},
\code{\link[=microeda_qc_plot]{microeda_qc_plot()}}
}
