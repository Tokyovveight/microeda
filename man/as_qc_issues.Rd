% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{as_qc_issues}
\alias{as_qc_issues}
\title{Build a compact QC issues table}
\usage{
as_qc_issues(x)
}
\arguments{
\item{x}{A \code{microeda_qc} object.}
}
\value{
A data frame with columns \code{issue_type}, \code{issue_id}, \code{category},
\code{severity}, \code{message}, \code{flag_id}, and \code{observation_id}. If no issues are
present, the data frame has zero rows and the same columns.
}
\description{
\code{as_qc_issues()} combines issue-like rows from a \code{microeda_qc} object into a
stable data frame for downstream filtering and display. It includes
\code{x$qc_flags} and non-info rows from \code{x$qc_observations}.
}
\examples{
counts <- matrix(
  c(0, 0, 1, 0),
  nrow = 2,
  byrow = TRUE
)
rownames(counts) <- c("S1", "S2")
colnames(counts) <- c("ASV1", "ASV2")

qc <- microeda_qc(counts, taxa_are_rows = FALSE)
as_qc_issues(qc)
}
